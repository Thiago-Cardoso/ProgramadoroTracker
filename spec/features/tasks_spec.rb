require 'rails_helper'

RSpec.feature "Feature Tasks", type: :feature, js: true do
  
  # it '#create' do
    # visit(new_task_path)
    # find('select.task_category_id').find("option[value='50']").select_option
    # select('50', :from => 'select.task_category_id', :visible => 'true')
    # select('1', from: 'task[category_id]')
    # fill_in('task[description]', with: "tarefa1")
    # click_button("Inserir")
    # expect(page).to have_content(true)
  # end
end
